/**
 * AetherMux — orchestrator entry point.
 *
 * Phase 1 scaffold only. The connection router, sandbox manager, agent spawn
 * contract, and session persistence are implemented in child issues of the
 * Phase 1 epic (AETHERMUX-1). This module currently exposes only package
 * metadata so build/test wiring can be verified end-to-end.
 */

export const VERSION = '0.0.0';

export const PRODUCT = 'AetherMux';
